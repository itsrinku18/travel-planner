import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Brand palette (from requirements)
  static const travelBlue = Color(0xFF0B5FFF);
  static const oceanTeal = Color(0xFF00C2A8);
  static const sunsetGold = Color(0xFFFFB703);

  static const background = Color(0xFFF7FAFC);
  static const surface = Color(0xFFFFFFFF);

  static const darkBackground = Color(0xFF0F172A);

  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);

  static const success = Color(0xFF10B981);
  static const error = Color(0xFFEF4444);

  // Glass
  static const glassTintLight = Color(0x99FFFFFF); // 60%
  static const glassTintDark = Color(0x330B1220); // subtle dark tint
}
